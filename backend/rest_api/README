To deploy, you'll need to login to gcloud:

 - gcloud auth login [ACCOUNT]

and run gcloud app deploy 

alternatively, ./deploy.sh or ./debug.sh should do everything. To use these, run 'chmod +x deploy.sh' or 'chmod +x debug.sh' to add run permissions. and then execute


